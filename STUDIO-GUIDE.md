# Rolin Studio / 使用说明

## 先在本地整理

在 Mac 上打开项目中的 **Start Studio.command**。需要现有 Node.js 环境；首次缺少依赖时会安装项目已有依赖。终端出现本地地址后，在浏览器打开它。默认从 `http://127.0.0.1:4380/` 开始，端口占用时自动尝试下一个。

同级输出的 `rolin-studio-preview.html` 只是可浏览的界面预览，不能替代本地服务，也不会假装保存或上传成功。

**保存到本地、添加素材、生成预览均不会连接上传 API。** 不连接 GitHub 也可以完成这些工作。关闭服务会清除内存中的 GitHub 凭据，不会清除本地作品和文件。

## 一份作品，只填一遍

| 你准备的内容 | 工作台位置 | 网站中的去向 |
| --- | --- | --- |
| 名称、适合谁、解决什么问题 | 作品介绍 | 作品卡、完整目录、详情标题 |
| 使用过程、输入输出、注意事项 | 详细介绍 | 点击作品后的详情 |
| 一张代表性截图或成果图 | 封面 | 首页重点作品卡 |
| 多张过程截图及说明 | 截图与封面 | 详情中的图文内容 |
| 产品网址、视频演示、源码地址 | 作品入口 | 可点击的入口 |
| SKILL.md、工作流 JSON、ZIP 安装包 | 下载文件 | 默认仅在本地；公开 Release 后变成下载入口 |
| 是否出现在首页大图区域、排序 | 展示位置 | 首页最多四个重点作品 |

工作流归入 Skills。未选择首页重点的作品仍会进入对应类别的完整目录。工作台不会把每个新作品硬塞进人物首屏，也不会用下载包充当封面。
人物主视觉、分类小图、独立 Videos 栏目和 Build Log 栏目的编辑不在本轮作品编辑范围内；“首页各区域放什么”提供对应说明。

## 图片和下载包分开

- 图片：浏览器自动缩到长边最多 1800 px，并转换成 WebP；原图最多 25 MiB，服务端接收转换后最多 8 MiB。只接受 PNG、JPEG、WebP，不接受带脚本的 SVG/HTML。
- 下载文件：保留文件名，保存在 `.studio/downloads`，不进入 `public`，也不进入 Cloudflare 网站构建或 Git 提交。
- 选错本地下载文件可以在工作台移除副本，不影响你原来的文件。
- 当前不打包任意文件夹；多文件 Skill/工作流先准备好一个 ZIP，再选择它。

## 以后决定上传时

1. 点击“连接 GitHub”。在 GitHub 创建仅针对 `roldself/rolin-ai-lab` 的细粒度访问凭据，Repository permissions 的 Contents 设为 Read and write。凭据只填在本地工作台，不要发给聊天助手。
2. 要提供下载：选中本地文件，填版本说明，点“上传到 Release 草稿”，审阅清单并确认。这一步上传到 GitHub，但仍是草稿。
3. 再点击“确认公开”，审阅全部附件并确认。公开成功后，工作台把下载链接保存回本地作品。**这不会自动提交网站。**
4. 要更新网站：点“上传网站”，检查目标仓库、分支、文件数量和大小，再确认。工作台创建普通 Git 提交，不需要手工改 GitHub 文件，不强推、不删除无关远端文件。

保存、上传草稿、公开 Release、上传网站是四个独立动作。每个有外部影响的写入动作都由按钮确认触发，没有自动上传监听器。
上传中保持本地服务运行。网络错误可能留下未发布草稿；已确认的附件会保留，可重试。遇到未能校验的同名远端文件，不会擅自覆盖。

## Cloudflare 只绑定一次

在 Cloudflare Pages 中授权并选择这个 GitHub 仓库。构建命令为 `npm run build`，输出目录为 `dist`。此后工作台提交到已绑定的分支，Cloudflare 才会开始部署。
这项首次账户授权需要本人操作，不等于手动编辑代码。工作台当前确认的是 GitHub 提交结果，**不会把提交成功冒充 Cloudflare 部署成功**；部署结果需要在 Cloudflare 检查。

## 容量规则

截至本轮核实，Pages 免费版最多 20,000 个站点文件，单个静态资源最大 25 MiB；GitHub Releases 每个附件必须小于 2 GiB，每个 Release 最多 1,000 个附件。下载包不占用 Pages 资源额度。这里不是把 GitHub 普通仓库当大文件仓库。

来源：[Cloudflare Pages limits](https://developers.cloudflare.com/pages/platform/limits/)、[GitHub releases](https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases)、[GitHub 大文件限制](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)。

## 删除与预填内容

选中作品后，点击保存旁边的“删除作品”，确认后会从本地作品目录和首页推荐中移除。图片、下载文件和 GitHub 内容保留，不会自动上传。删除前的内容会备份到本地；后续保存可能更新这份备份。

现有 14 个作品已补充介绍、用途和能力方向。它们是可继续修改的内容底稿，不代表功能全部完成；真实截图、版本和体验链接没有猜填。

## 验证边界

已通过本地保存、备份、图片和下载分流、容量校验、确认门槛、保存到 Astro 构建结果的往返测试；GitHub 提交和 Release 链路使用模拟 API 测试。
没有连接真实账号、上传文件、发布 Release 或触发 Cloudflare 部署。图文升级已完成真实本地服务和浏览器中的保存、阅读预览、桌面与手机排版检查。
工作副本位于本输出项目，原目录 `/Users/ro/rolin-ai-lab` 没有写回或推送。

## 图文与个人资料

工作台左侧增加“作品 / 图文 / 个人资料”。原有 Skill、Workflow、Agent、Product 编辑方式不变。

在“图文”点击 + 创建文章，填写标题、摘要、日期和封面。正文通过“段落 / 标题 / 引用 / 图片”逐块添加；上下箭头调整顺序，图片下方可填写说明。“阅读预览”支持未保存的内容，不会上传。第一版为块式编辑，不是微信公众号富文本导入器。

草稿不出现在网站首页，也不生成公开阅读页面。选择“网站展示”并保存后，先预览网站，再回到作品页确认上传。状态变更本身不会联系 GitHub。上传到公开仓库时，草稿仍包含在源码中，不能把草稿当作私密存储。

“个人资料”可填写名称、介绍、联系邮箱、小红书账号及主页、抖音账号及主页、微信号及二维码。空字段不显示；填写的内容会用于页脚和文章底部联系区。不需要把联系方式发给助手，直接在本地表单中填写。

首页和文章页使用红绿鼠标烟雾代替原首屏水波纹。烟团从鼠标路径展开，逐渐膨胀并在约 3 至 4 秒内散去；触屏和系统减少动态效果时禁用，页面暂停动效按钮也能关闭。没有增加运行依赖。

如果旧服务仍在运行，新入口可能显示“此入口不存在”。先保存正在编辑的内容，再重启工作台。重启会清除服务内存中的 GitHub 凭据，不会清除已保存内容。
